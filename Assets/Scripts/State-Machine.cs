/*using UnityEngine;
using System.Collections;

public class State-Machine : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	//if (Application.isLoadingLevel == true && Application.loadedLevelName != "Loading") Application.LoadLevel("Loading");
	//Application.LoadLevel("LevelName");
	//Application.LoadLevel("LevelIndex");
}*/
